[package]
name = "core"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "backend"
path = "src/main.rs"

[dependencies]
utils = { path = "../utils" }
llm = { path = "../llm" }
toml = "1"
serde_json = "1"
